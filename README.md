# あろはーず公式ホームページ

女性2人組の踊ってみたユニット「あろはーず」のファン向け公式サイトです。

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

## Deploy

`main` ブランチに push すると、GitHub Actions から Firebase Hosting に自動デプロイされます。
