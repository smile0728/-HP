# Todo

## 優先度 高

- [x] SNSリンクを実際の公式 URL に差し替える。
- [x] 動画シアターを実 YouTube 埋め込みにするか、疑似プレイヤー仕様として明確に固定する。
- [ ] ギャラリーと動画サムネイルの `picsum.photos` プレースホルダー画像を公式素材に差し替える。
- [ ] Firebase プロジェクト、Firestore database ID、Google 認証プロバイダ、許可ドメインの本番設定を確認する。
- [ ] Firebase Authentication で匿名ログインプロバイダを有効化する。
- [ ] Firebase Console で初期管理者 UID を確認し、`admins/{uid}` ドキュメントを作成する。
- [x] Firestore ルールを Firebase にデプロイし、管理者以外が書き込みできないことを確認する。
- [ ] `users/{uid}/gacha/state` が本人以外から読書きできないことを確認する。
- [x] `daily_stats` の書き込みルールを悪用されにくい形に制限する。
- [x] 初期管理者の UID を Firestore の `admins/{uid}` に手動登録する運用手順を用意する。
- [ ] 本番でローカル模擬管理者ログインが表示されないよう、モック判定と設定値を確認する。
- [x] `.env.example` と README の Gemini API 記述が現状実装に必要か確認し、不要なら整理する。
- [x] `vite.config.ts` のコメント文字化けを修正する。

## 優先度 中

- [x] 交換日記コメントの削除操作に確認 UI を追加する。
- [x] コメント投稿時のバリデーションエラーを画面に表示する。
- [x] ガチャの「テスト中の最初からコンプやり直し」操作を本番向けに非表示化する。
- [ ] ガチャの1日1回制限をローカルストレージ依存でよいか確認する。
- [ ] おみくじ画像生成時に長いテキストが枠を超えないか検証し、必要なら折り返しとフォントサイズ調整を強化する。
- [x] コンプリート条件を `collection.length >= 6` ではなく、全 `luckLevel` の実収集判定にする。
- [ ] 動画の推し数と音楽プレイヤーのいいね数を永続化するか決める。
- [x] `CursorSparks` の古いパーティクル削除ロジックを時刻ベースに見直す。
- [x] `/admin` ルーティングを `react-router-dom` で正式に扱うか、現状の簡易ルーティングのままにするか決める。
- [ ] 管理画面フォームの入力値に対して、保存前の必須チェックと文字数エラー表示を強化する。
- [x] 管理画面の削除操作に確認ダイアログを追加する。
- [ ] Firebase 失敗時のローカルフォールバックが本番利用者に見えてよいか確認する。
- [x] 管理画面で編集したガチャ結果を公開側のおみくじに反映するか、現状の固定 `FORTUNES` を継続するか決める。
- [x] 管理画面で編集したコンプリート手紙を公開側のレター表示に反映するか決める。
- [x] 管理画面の保存成功/失敗を `alert` ではなく画面内トーストやステータス表示に置き換える。
- [ ] 管理画面の一覧が多くなった場合のページングまたは検索を追加する。

## 優先度 低

- [ ] プロフィール画像を絵文字プレースホルダーから公式イラストまたは写真に差し替える。
- [ ] 交換日記をデータ追加しやすい CMS または JSON 管理に移行する。
- [ ] ギャラリー写真のカテゴリー、説明文、ステッカーを `data.ts` 側に移す。
- [ ] Web Audio API の音量、音色、停止処理を共通ユーティリティ化する。
- [x] アニメーションを抑える設定を用意する。
- [x] キーボード操作とフォーカス表示を強化する。
- [x] ライトボックスを Escape キーで閉じられるようにする。
- [ ] ガチャ、コメント、ライトボックスの基本操作テストを追加する。
- [ ] 管理画面のログイン、CRUD、公開/非公開切り替えのテストを追加する。
- [ ] Firebase データ取得部分を共通の loading/error UI に寄せる。
- [x] 大きくなったバンドルを `AdminPanel` の遅延読み込みで分割する。
- [x] `react-router-dom` を使わないなら依存から外すか、正式にルーティングへ導入する。

## 確認事項

- [x] ユニット名の正式表記は「あろはーず」で統一済み。
- [x] メンバー名の英語表記は `Smile` / `Kyarumen` で統一済み。
- [x] SNS、YouTube、TikTok、X の公式 URL を確認する。
- [ ] 公式素材として使う画像、ロゴ、動画サムネイルの権利と配置場所を確認する。
- [x] コメント機能はローカル保存のみでよいか、共有掲示板化するか決める。
- [ ] 匿名アカウントの別端末引き継ぎを Google ログイン連携で対応するか決める。
- [ ] お知らせ、日記、写真、ガチャ、手紙の初期データは Firestore へ投入するか、フォールバックデータを正式採用するか決める。
